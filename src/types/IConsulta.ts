import IMKE from "./IMIKE";

export default interface IESCALA extends IMKE {
    id: string,
    data: string,
    horario: string,
    especialidade: string,
    paciente: string,
    modalidade: string
}