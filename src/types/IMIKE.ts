import IEndereco from "./IEndereco";

export default interface IMike {
    id?: string,
    grad?: string,
    re?: string,
    dig?: string,
    nome?: string,
    qra?: boolean,
    senha?: string,
    equipe?: string[],
    createdAt?: boolean,
    updatedAt?: string
}